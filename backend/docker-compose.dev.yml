version: '3.8'

# Docker Compose для разработки (только база данных)
services:
  postgres:
    image: postgres:16-alpine
    container_name: cte-postgres-dev
    environment:
      POSTGRES_DB: cte_grouping
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_dev_data:
    driver: local

